<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.seocheon.fitian.mapper.MembershipHistoryMapper">
    
    <select id="findByUid" resultType="MembershipHistoryModel">
        select *
        from fitian.Membership_history
        where membershipNo = #{membershipNo}
    </select>
    
    <insert id="createHistory">
    	insert into fitian.Membership_history (membershipNo, actionType, prevValue, newValue, performedBy, memo, period) 
    	value (#{membershipNo},#{actionType},#{prevValue},#{newValue},#{performedBy},#{memo}, #{period})
    </insert>
   
</mapper>

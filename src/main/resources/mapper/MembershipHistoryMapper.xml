<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.seocheon.fitian.mapper.MembershipHistoryModel">
    
    <select id="findByUid" resultType="MembershipHistoryModel">
        select *
        from fitian.Membership_history
        where membershipNo = #{membershipNo}
    </select>
    
    <insert id="createHistory">
    	insert into fitian.Membership_history (membershipNo, actionType, preValue, newValue, performedBy, memo) 
    	value (#{membershipNo},#{actionType},#{preValue},#{newValue},#{performedBy},#{memo})
    </insert>
   
</mapper>
